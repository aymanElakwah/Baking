<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/frame_padding">

    <android.support.v7.widget.CardView
        android:id="@+id/recipe_card_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/recipe_image"
                android:layout_width="@dimen/image_food_width"
                android:layout_height="@dimen/image_food_height"
                android:layout_marginRight="@dimen/image_food_padding"
                android:layout_marginLeft="@dimen/image_food_padding"
                android:layout_marginTop="@dimen/image_food_padding"
                android:adjustViewBounds="true"
                tools:srcCompat="@drawable/default_food_image" />

            <TextView
                android:id="@+id/recipe_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:textAppearance="@style/TextAppearance.AppCompat.Caption"
                android:textSize="24sp"
                tools:text="Nutella Cake" />
        </LinearLayout>
    </android.support.v7.widget.CardView>
</FrameLayout>